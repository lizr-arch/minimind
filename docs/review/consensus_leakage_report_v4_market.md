# Consensus Leakage Report

Generated: 2026-06-24T10:21:15.245874

Input: `data/odds_real/titan007_pure_v4_market_semantics.jsonl`
Splits dir: `data/odds_real/splits/v4_market`
Consensus mode: `none`

## 1. Data Overview
- Total matches: 1491
- Total events: 54631
- Matches with `bookmaker_features`: 0
- Events with real euro odds: 54631
- Train matches: 1032
- Val matches: 221
- Test matches: 221

## 2. Consensus Feats Generation Location
- **Where**: `dataset/odds_dataset.py` → `_compute_consensus_feats()`
- **When**: Called from `_build_features_and_labels()` for each dataset item
- **Input**: Depends on `consensus_mode` parameter

## 3. Current Data: bookmaker_features Status
- **Result**: NO matches have `bookmaker_features` in the JSONL data
- **Implication**: `consensus_feats` is ALWAYS `zeros(6)` regardless of mode
- **Leakage risk**: NONE — zeros contain no information

## 4. Consensus Mode Analysis
- Current mode: `none`
- Available modes: ('none', 'visible_only', 'legacy_full_timeline')
- `none`: Returns `zeros(6)` — safe, no leakage
- `visible_only`: Computes from cutoff-filtered timeline — cutoff-safe
- `legacy_full_timeline`: Uses full-match bookmaker_features — **DEBUG ONLY, leaks future data**

## 5. Cutoff Comparison
| Cutoff | All Zero | Mean[0] | Count |
|--------|----------|---------|-------|
| 1440 | True | 0.000000 | 8 |
| 60 | True | 0.000000 | 10 |
| 1 | True | 0.000000 | 10 |

## 6. Full Timeline vs Cutoff-Filtered Comparison
- `build_cutoff_samples()` does NOT copy `bookmaker_features` to cutoff samples
- Even if full-match had bookmaker_features, cutoff samples would have none
- This is an additional safety layer against leakage

## 7. Missing Data
- `bookmaker_features` field: **ABSENT** from all matches
- `consensus_feats` output: **ALWAYS zeros(6)**
- Closing odds: Not used for consensus computation

## 8. Leak Checks
- Uses full timeline consensus?: **SAFE**
- Uses closing odds?: **SAFE**
- build_cutoff_sample copies full-match consensus?: **SAFE**
- Collator generates its own consensus?: **SAFE**
- Dataset item missing consensus → zeros?: **SAFE**
- cutoff=1440/60/1 produce same consensus?: **SAFE**

## 9. Verdict

**PASS**
- consensus_mode is `none` (default)
- No bookmaker_features in data
- consensus_feats is always zeros(6)
- No future information leakage possible

---
*Report generated by `tools/report_consensus_leakage.py`*